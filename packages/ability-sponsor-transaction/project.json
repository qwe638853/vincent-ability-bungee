{
  "name": "ability-sponsor-transaction",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/ability-sponsor-transaction/src",
  "projectType": "library",
  "targets": {
    "clean": {},
    "action:build": {
      "executor": "nx:run-commands",
      "dependsOn": ["policy-counter:build"],
      "options": {
        "commands": ["pnpm node ./scripts/bundle-vincent-ability/esbuild.js"],
        "cwd": "packages/ability-sponsor-transaction",
        "parallel": false
      }
    },
    "action:only:deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "{projectRoot}"
      }
    },
    "action:deploy": {
      "executor": "nx:run-commands",
      "dependsOn": ["action:build"],
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "packages/ability-sponsor-transaction"
      }
    },
    "build": {
      "executor": "@nx/js:tsc",
      "dependsOn": ["action:build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/ability-sponsor-transaction/dist",
        "main": "packages/ability-sponsor-transaction/src/index.ts",
        "tsConfig": "packages/ability-sponsor-transaction/tsconfig.lib.json",
        "assets": [
          "packages/ability-sponsor-transaction/*.md",
          {
            "glob": "**/*",
            "input": "packages/ability-sponsor-transaction/src/generated",
            "output": "src/generated"
          }
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "{projectRoot}/jest.config.js",
        "passWithNoTests": true
      }
    }
  }
}
